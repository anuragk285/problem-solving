# Next greater numerically balanced number

```python
class Solution(object):
    def nextBeautifulNumber(self, n):
        """
        :type n: int
        :rtype: int
        """
        def is_num_balanced(n):
            n = str(n)
            freq = {}
            for i in n:
                if n.count(i) != int(i):
                    return False
            return True
        for i in range(n+1, 1224445):
            if is_num_balanced(i):
                return i
        return -1
```
