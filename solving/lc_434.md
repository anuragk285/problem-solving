# Number of segments in a string

```java
class Solution {
    public int countSegments(String s) {
        s = s.trim();
        if(s.equals("")) return 0;
        String[] strs = s.split("\\s+");
        for(String sr : strs) System.out.println(sr);
        return strs.length;
    }
}
```
